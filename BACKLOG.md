# BACKLOG.md — สิ่งที่ยังไม่เสร็จ ส่งต่อ Module 3

> ของส่งมอบชิ้นที่ 5 ของ [การบ้านที่ 4 (Module 2 สัปดาห์ที่ 9)](https://cnacha-mfu.github.io/raise2-module2/materials/week9/w9-homework.html)
> บันทึกตามความจริง ณ **20 กันยายน 2569** — ไม่ตกแต่งให้ดูเสร็จกว่าที่เป็น
>
> อ้างอิง: [`spec.md`](spec.md) · [`test-results.md`](test-results.md)

---

## 🔴 1. ความปลอดภัย — ทำก่อนอย่างอื่น

### 1.1 ย้ายคีย์ AI ไปไว้ฝั่งที่ผู้ใช้แตะไม่ได้ ⬅️ สำคัญที่สุด

**สถานะตอนนี้:** คีย์ยังอยู่ฝั่งเบราว์เซอร์ แต่ **ไม่อยู่ใน repo และไม่ถูก deploy ขึ้นเว็บ**

| | ก่อนสัปดาห์ที่ 9 (repo เดิม) | ตอนนี้ | เป้าหมาย Module 3 |
|---|---|---|---|
| อยู่ใน GitHub? | ❌ ไม่อยู่ (`.gitignore`) | ❌ ไม่อยู่ | ❌ ไม่อยู่ |
| โหลดจากเว็บสาธารณะได้? | ⚠️ **ได้** — `/config.local.js` ตอบ HTTP 200 | ✅ ไม่ได้ (กันใน `firebase.json`) | ✅ ไม่ได้ |
| ไปถึงเบราว์เซอร์ผู้ใช้? | ✅ ไป | ⚠️ **ยังไป** (ผู้ใช้วางคีย์เอง) | ❌ ไม่ไปเลย |

**ยังต้องทำ:**
- [ ] ย้ายการเรียก OpenRouter ไปไว้หลังบ้าน (Cloud Function / Cloud Run) — คีย์อยู่ใน
      environment config ฝั่งเซิร์ฟเวอร์ ไม่ผ่านเบราว์เซอร์เลย **ต้องใช้ Blaze plan**
- [ ] ให้ฟังก์ชันหลังบ้านตรวจ Firebase ID token ก่อนเรียก AI ทุกครั้ง
- [ ] ใส่ rate limit ต่อผู้ใช้ กันคนกดรัว ๆ จนค่าใช้จ่ายบาน
- [ ] ลบเส้นทาง "ถามผู้ใช้ให้วางคีย์" ใน `public/ai-key.js` ทิ้งเมื่อมีหลังบ้านแล้ว

### 1.2 คีย์เดิมที่เคยเปิดสาธารณะต้องถือว่าใช้ไม่ได้แล้ว

- [ ] แจ้งผู้สอนว่าคีย์ของหลักสูตรเคยถูกเสิร์ฟที่
      `https://sattasarasada-perfume.web.app/config.local.js` ตั้งแต่ deploy ของสัปดาห์ที่ 8
      จนถึง 20 ก.ย. 2569 — ควรออกคีย์ใหม่ให้ทั้งรุ่น
- [ ] เปลี่ยนไปใช้คีย์ใหม่ใน `config.local.js` ของเครื่องตัวเอง

> 💡 **บทเรียน:** `.gitignore` กันได้แค่ GitHub — ไม่ได้กันการ deploy
> ไฟล์ที่ถูกกันไม่ให้ commit ยังขึ้นเว็บได้ถ้า `firebase.json` ไม่กันไว้อีกชั้น

### 1.3 จุดที่ rules ยังหย่อน

- [ ] `users` ทุกคนที่ login แล้วอ่านได้หมด — ผู้ใช้คนหนึ่งอ่านอีเมลของคนอื่นได้ถ้ารู้ uid
      ควรจำกัดให้อ่านได้เฉพาะของตัวเองกับของ QC
- [ ] ยังไม่มีกลไกกันการเขียน `aiLog` รัว ๆ จนเปลือง quota

---

## 🧪 2. สิ่งที่เทสต์จับได้แต่ยังไม่ได้แก้

ดูผลล่าสุดที่ [`test-results.md`](test-results.md) เสมอ (สร้างจากผลรันจริงอัตโนมัติ)

### เทสต์ที่ควรเพิ่มใน Module 3

- [ ] QC กดอนุมัติ/ตีกลับแล้วสถานะเปลี่ยนถูกต้อง (ต้องมีบัญชี `qc_reviewer` สำหรับทดสอบ)
- [ ] Perfumer พยายามเปลี่ยน `role` ตัวเองเป็น `qc_reviewer` แล้วต้องถูกปฏิเสธ
- [ ] Perfumer พยายามแก้สูตรตอนสถานะ `submitted` แล้วต้องถูกปฏิเสธ
- [ ] ปุ่มลบ: กดยืนยันแล้วลบจริง และลบสูตรที่ไม่ใช่ `draft` ไม่ได้
- [ ] ปุ่ม AI: กรณีไม่มีคีย์ / คีย์ผิด / timeout 15 วินาที ต้องไม่ทำให้หน้าค้าง

---

## 📋 3. ฟีเจอร์จาก Feature List ของ Module 1 ที่ยังไม่ได้ทำ

ตัดออกจากขอบเขต Module 2 ตั้งแต่ [`SCOPE.md`](SCOPE.md) — ไม่ใช่ของที่ "ลืมทำ"
Feature List ฉบับเต็มอยู่ใน repo เดิม `PerfumeRaisework` ที่ `docs/02-design/feature-list.md`

### 🔴 Must have ที่ยังไม่ได้ทำ (งานหลักของ Module 3+)

| รหัส | ฟีเจอร์ | หมายเหตุ |
|---|---|---|
| F-02 | Engine A — เครื่องคำนวณฟิสิกส์เคมี | หัวใจของระบบจริง ยังไม่ได้เริ่ม |
| F-03 | Olfactory Threshold Filter (ODT) | ต้องมีฐานข้อมูลค่า ODT ก่อน |
| F-04 | Engine B — บรีฟบรรยายกลิ่นอัตโนมัติ | ตอนนี้มีแค่ปุ่ม AI สรุปแบบหยาบ ยังไม่ได้อิงผลจาก Engine A |
| F-05 | IFRA Compliance Check ของจริง | ตอนนี้เป็นข้อสังเกตเชิงคุณภาพจาก AI ไม่ได้เทียบเพดานจริง |
| F-06 | Muddy Accord Detection | ยังไม่ได้เริ่ม |
| F-07 | Dashboard สรุปผลสูตร + พีระมิดกลิ่น | ยังไม่ได้ต่อข้อมูลจริง |
| F-18 | การป้องกันสารต้องห้าม | ต้องมีฐานข้อมูลสถานะ IFRA ก่อน |
| F-08 | Audit Log + PDPA consent | ทำแล้วบางส่วน (auth + role) ยังขาด audit log และการจัดการความยินยอม |

### 🟠 Should / 🟡 Could ที่ยังไม่ได้ทำ

F-09 Fine-tune รายสาร · F-10 คำแนะนำแก้ไข + เตือน Scent Drift · F-11 Visualization + Command Bar ·
F-12 What-If Simulation · F-13 Member-Level Distinctiveness · F-14 คำนวณต้นทุนสูตร ·
F-15 ประวัติเวอร์ชันสูตร · F-19 มุมมองภาพรวมหลายสูตร · F-16 Import/Export · F-17 คลังสารส่วนตัว

---

## 🔧 4. หนี้ทางเทคนิคของระบบเล็กนี้

- [ ] **แก้ไขสูตรหลังบันทึกไม่ได้** — สร้างแล้วแก้ไม่ได้ ต้องลบแล้วสร้างใหม่
- [ ] **แก้ไข/ลบวัตถุดิบรายตัวไม่ได้**
- [ ] **ไม่มีหน้าจัดการ `fragranceTypes`** — seed ผ่านสคริปต์อย่างเดียว
- [ ] **ตั้ง role เป็น `qc_reviewer` ต้องแก้มือใน Firebase Console** ยังไม่มีหน้าแอดมิน
- [ ] **ข้อมูล seed ใช้ `perfumerId` สมมติ** ที่ไม่ใช่ Auth UID จริง — สาธิตได้แค่มุมมอง QC
- [ ] **ไม่มี pagination** ในหน้ารายการ
- [ ] **ไม่มี CI** — เทสต์ต้องรันด้วยมือ ยังไม่มี GitHub Actions รันอัตโนมัติทุก push
- [ ] **repo นี้ยังไม่ได้ deploy ทับของเดิม** — ใช้ Firebase project เดียวกัน
      (`sattasarasada-perfume`) ต้องตัดสินใจก่อนว่าจะให้ repo ไหนเป็นตัวที่ deploy จริง
