# CLAUDE.md

ไฟล์นี้ให้คำแนะนำแก่ Claude Code เมื่อทำงานกับโค้ดใน repo นี้

## repo นี้คืออะไร

**Formula Review** — ระบบเก็บสูตรน้ำหอมที่นักปรุงสร้างขึ้น ส่งตรวจ แล้วให้ QC อนุมัติ/ตีกลับ
สร้างขึ้นสำหรับ **Module 2 การบ้านที่ 4 (สัปดาห์ที่ 9) ส่วน A ข้อ 3 — "สั่งทีเดียวจบ"**

โค้ดทั้ง repo นี้ถูกสร้างขึ้นใหม่ทั้งชุดจาก [`spec.md`](spec.md) ในคำสั่งเดียว
โดยผู้ช่วย AI 3 ตัวใน [`.claude/agents/`](.claude/agents/) ที่แบ่งงานกันตามความเสี่ยง
**ไม่ได้คัดลอกโค้ดมาจาก repo เดิม** — จุดประสงค์คือพิสูจน์ว่าสเปคชัดพอจะสร้างระบบขึ้นใหม่ได้

repo ต้นทาง (มีเอกสาร requirements/design เต็มระบบและบันทึกงานสัปดาห์ที่ 6–9):
https://github.com/sattasarasadaw-crypto/PerfumeRaisework

## ⚠️ แหล่งความจริงเดียวคือ `spec.md`

**เปิดอ่าน [`spec.md`](spec.md) ก่อนตอบคำถามหรือแก้โค้ดใด ๆ เกี่ยวกับระบบนี้เสมอ**
ห้ามสมมติฟีเจอร์หรือชื่อฟิลด์จากความจำหรือจากโปรเจกต์อื่น สิ่งที่ต้องยึดจากสเปค:

- **หน้าจอมี 5 หน้าเท่านั้น** (หัวข้อ 3) — `signup` / `login` / `index` / `formula-new` / `formula-detail`
- **`formulas.status` มีได้แค่ 4 ค่า:** `draft` → `submitted` → `approved` หรือ `rejected`
- **`users.role` มีได้แค่ 2 ค่า:** `perfumer` (ค่าเริ่มต้นตอนสมัคร) และ `qc_reviewer` (ตั้งมือใน Console เท่านั้น)
- **หัวข้อ 6 คือรายการสิ่งที่ "ไม่ทำ"** — ถ้าเผลอสร้างของในรายการนั้นขึ้นมา ถือว่าผิดสเปค ให้เอาออก

สิทธิ์การเข้าถึงฉบับเต็มอยู่ที่ [`ACL.md`](ACL.md) และถูกบังคับใช้จริงที่ [`firestore.rules`](firestore.rules)

## กติกาโดเมนที่ห้ามละเมิด

- **AI แปลผลเป็นคำบรรยายเท่านั้น ห้ามสร้าง/แก้ตัวเลขทางเคมีเอง**
- **ห้ามให้ AI ฟันธงว่าควรอนุมัติหรือตีกลับ** และห้ามอ้างตัวเลขขีดจำกัด IFRA ราวกับเป็นผลตรวจทางการ
- **มนุษย์ตัดสินใจขั้นสุดท้ายเสมอ** — ปุ่ม AI ห้ามแตะฟิลด์ `status` เด็ดขาด
- ผลลัพธ์จาก AI ต้องมีป้ายกำกับชัดเจนทุกครั้งว่ามาจาก AI

## 🔒 กติกาเรื่องคีย์ (บทเรียนที่แลกมาด้วยคีย์หลุดจริง)

คีย์ OpenRouter ของ repo เดิม **เคยเปิดอ่านได้สาธารณะ** ที่
`https://sattasarasada-perfume.web.app/config.local.js` เพราะ `.gitignore` กันได้แค่ GitHub
แต่ **ไม่ได้กันการ deploy** — Firebase Hosting ดูที่ `firebase.json` คนละไฟล์กัน

ดังนั้นใน repo นี้:

- `config.local.js` ต้องอยู่ทั้งใน **`.gitignore`** และใน **`firebase.json` → `hosting.ignore`** เสมอ
  **ห้ามเอาออกจากที่ใดที่หนึ่งเด็ดขาด**
- คีย์ต้องมาจาก `getOpenRouterKey()` ใน [`public/ai-key.js`](public/ai-key.js) เท่านั้น
  ห้าม import คีย์แบบ static ที่หัวไฟล์ (ต้องเป็น dynamic `import()` ในตัวจัดการปุ่ม
  เพื่อให้คีย์หาย = พังแค่ปุ่ม AI ไม่ใช่ทั้งหน้า)
- ห้ามบันทึกคีย์ลง Firestore หรือ log ใด ๆ และห้ามส่งไปไหนนอกจาก OpenRouter
- มีเทสต์ T6 ใน `tests/security.spec.js` คอยยิงเช็กเว็บจริงว่าไม่มีคีย์หลุด — **ห้ามลบหรือผ่อนเทสต์นี้**

## 🔒 ขอบเขตข้อมูล

repo นี้ถูก push ขึ้น GitHub — **ห้ามนำเข้ามาเด็ดขาด**: เอกสารสัญญา/MoU/โครงสร้างหุ้น,
เอกสารธรรมาภิบาล/IP, ข้อมูลส่วนบุคคลของผู้เกี่ยวข้อง (ชื่อจริง เบอร์ อีเมล ที่อยู่),
ฐานข้อมูลสารเคมีดิบ ราคาวัตถุดิบ CAS Number เต็ม, credential/token/คีย์ใด ๆ
ข้อมูลตัวอย่างทั้งหมดใน `seed.js` เป็นข้อมูลสมมติเพื่อสาธิตเท่านั้น

## คำสั่งที่ใช้บ่อย

```bash
npm install                  # ติดตั้ง dependency
npx playwright install chromium
npm test                     # รันชุดทดสอบยิงเว็บจริง แล้วสร้าง test-results.md อัตโนมัติ
npm run test:security        # รันเฉพาะเทสต์ความปลอดภัย
npm run seed                 # ใส่ข้อมูลตัวอย่างลง Firestore (ระวัง: เขียนลงฐานข้อมูลจริง)
firebase deploy --only hosting,firestore:rules
```

**ไม่มี lint** ใน repo นี้ · ชุดทดสอบยิงไปที่เว็บจริงที่ deploy แล้ว ไม่ใช่ไฟล์ในเครื่อง

## ผู้ช่วย AI ใน `.claude/agents/`

| ผู้ช่วย | ดูแล | โมเดล |
|---|---|---|
| `formula-ui-builder` | หน้าจอ HTML ใน `public/` | haiku |
| `formula-data-rules` | Firestore + `firestore.rules` + `seed.js` | opus |
| `formula-ai-feature` | ปุ่ม AI + `ai-key.js` | sonnet |
| `formula-tester` | ชุดทดสอบใน `tests/` | sonnet |

เมื่อถูกขอให้แก้งานที่ตรงกับหน้าที่ของตัวไหน **ให้เรียกผู้ช่วยตัวนั้นแทนการแก้ไฟล์เอง**
ทุกตัวมีหัวข้อ "🚫 ห้ามทำงานนอกสเปค" กำกับว่าห้ามแตะไฟล์ของใครและห้ามเพิ่มของที่สเปคไม่ได้ขอ
